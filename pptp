#!/usr/bin/env bash

set -e

UserName=""
PassWord=""

echo "${UserName} * ${PassWord} *" > \
  $HOME/chap-secrets

docker run \
  -d \
  --name=PPTP \
  --privileged \
  --net=host \
  -v $HOME/chap-secrets:/etc/ppp/chap-secrets \
  mobtitude/vpn-pptp
